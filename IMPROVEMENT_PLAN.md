# 夜市消消樂改善企劃

建立日期：2026-09-23  
狀態：P2 實作與自動驗證完成，待使用者驗收；P3～P6 尚未開始

## 目標與執行原則

讓玩家能看懂盤面、理解特殊規則，並透過選擇消除目標、保留特殊方塊與組合引爆取得成果；完成五關後，仍有可持續挑戰的內容。

- 保留原生 HTML、CSS、JavaScript 與現有夜市美術，不導入大型框架。
- 按 P1 → P6 執行。每階段交付可玩的版本、變更摘要、驗證結果及待驗收清單。
- 階段內自主完成實作與必要修正；完成後提供驗收，經使用者確認再進下一階段。驗收不通過時留在當階段修正。
- 新增內容以本文件為範圍依據；數值是可調整的初始方案，不視為已驗證的平衡結果。
- 舊有五關解鎖進度須保留。新存檔採版本欄位，損毀或不可用時提供安全預設。
- 手機觸控與桌面滑鼠都是主要操作方式；以窄螢幕及一般桌面尺寸檢查。

## 階段總覽

| 階段 | 主題 | 可驗收成果 | 相對工作量 |
| --- | --- | --- | --- |
| P1 | 共用引擎與穩定性 | 既有五關穩定運作、無解可復原 | 大 |
| P2 | 棋盤辨識與特殊特效 | 不看說明也能區分食物與特殊方塊 | 中 |
| P3 | 互動教學重做 | 使用正式規則完成滑動、四消、五消教學 | 中 |
| P4 | 計分、訂單與五關難度 | 五關有不同策略與漸進難度 | 大 |
| P5 | 無限夜市巡迴 | 第五關後可持續闖關、存檔與續玩 | 大 |
| P6 | 重玩誘因與整體驗收 | 星等、紀錄、提示與完整遊玩流程 | 中 |

## P1：共用引擎與穩定性

### 實作範圍

1. 從 `index.html` 抽出棋盤資料、配對、交換、特殊效果、掉落補牌與結算邏輯，與畫面、音效分離。
2. 統一普通消除與特殊交換的結算流程。方塊資料統一格式，避免普通數字與特殊物件混用。
3. 加入每局識別與流程取消：離開、重試、換關後，舊動畫不得修改新棋盤、分數或結果畫面。
4. 檢查合法交換；開局與每次連鎖結束時，無解則自動洗牌。保留特殊方塊、不扣步、不憑空計分；洗牌需有嘗試上限與可恢復策略。
5. 修正雙炸彈波及其他特殊方塊時未展開效果的問題，避免同一特殊方塊重複發動。
6. 支援固定盤面及可重現的亂數種子，供教學、規則驗證與難度測試使用。
7. 處理取消觸控與重複輸入；動畫期間不接受第二次交換。

### 本階段固定的特殊規則

- 三消正常清除；直線四消生成章魚燒炸彈；直線五消以上生成彩虹棉花糖。
- T／L 形本版不新增專屬特殊物件；依各直線配對判定，重疊格只計一次清除。
- 炸彈保留食物種類，可參與同類配對；與相鄰方塊交換即發動，以交換後位置清除 3×3。
- 棉花糖是無食物種類的特殊物件，不參與普通三消；與普通食物交換時清除該種類。
- 棉花糖被爆炸波及時，清除當下數量最多的普通食物種類；同數量依固定種類順序決定。
- 雙炸彈清除兩個 3×3 範圍的聯集，並觸發範圍內既有特殊方塊。
- 雙棉花糖清除全盤可清除方塊；棉花糖＋炸彈清除炸彈所屬食物種類並引爆該炸彈，暫不加入整批轉炸彈。
- 新生成特殊方塊在當次清除波保留，下一波起才可觸發，讓生成結果可理解。
- 無效交換退回且不扣步；一次有效交換及其全部連鎖只扣一步，最後一步先判定勝利再判定失敗。

### 驗收清單

- [ ] 三消、四消、五消、交叉配對、重力與補牌符合上述規則。
- [ ] 普通交換、各種特殊交換、特殊連鎖都有固定盤面驗證。
- [ ] 人工無解盤面會洗牌，特殊方塊保留，且洗牌後可操作。
- [ ] 連續返回首頁、重開、換關不出現幽靈加分、錯誤結算或鎖死。
- [ ] 原五關、音樂、關卡解鎖與舊存檔仍可使用。
- [ ] 直接開啟 `index.html` 與使用靜態伺服器均能運作；模組拆分不可破壞原有啟動方式。

## P2：棋盤辨識與特殊特效

### 實作範圍

1. 統一方塊視覺結構為底色、食物、特殊標記與效果層。
2. 為珍奶、地瓜球、臭豆腐、雞排、蚵仔煎配置紫、金黃、青綠、珊瑚紅、藍色底色；以 20～30% 透明度起始調整。
3. 保留食物輪廓；炸彈呈現所属食物底色與小徽章，棉花糖使用獨立彩虹外觀。
4. 炸彈加入脈衝光圈與少量火星；棉花糖加入旋轉彩虹環。生成、待機、發動三種效果清楚區分。
5. 爆炸顯示範圍；同類清除顯示目標連線。拖曳特殊方塊時提供發動範圍或目標預覽。
6. 控制粒子總量，提供減少動態效果設定並尊重系統偏好；降低效果時仍保留功能辨識。

### 驗收清單

- [ ] 桌面及 360px 寬手機畫面可區分五種食物，底色不遮蔽圖案。
- [ ] 炸彈、棉花糖與普通食物可快速辨識；炸彈的食物屬性可見。
- [ ] 特效實際範圍與引擎清除範圍一致。
- [ ] 全盤清除與多特殊連鎖不會殘留特效、遮住下一步操作或明顯卡頓。
- [ ] 減少動態效果後，仍可知道特殊方塊生成及發動的位置。

## P3：互動教學重做

### 實作範圍

1. 教學共用 P1 引擎、P2 方塊與手勢，不再硬寫消除結果或 Combo 等級。
2. 採固定盤面與預設補牌序列，依序教導：滑動三消 → 無效交換 → 製造並使用炸彈 → 製造並使用棉花糖 → 自由小練習。
3. 每步只突出當前操作，提供滑動手勢、短句說明、步驟進度與重試。
4. 示範真實掉落連鎖，說明有效交換扣一步、連鎖不額外扣步。
5. 提供略過、重玩及完成後開始第一關；記錄教學完成狀態，首次開始遊戲引導進教學，既有通關玩家不強制重做。
6. 特殊組合留給後續關卡的首次提示，不塞入基礎教學。

### 驗收清單

- [ ] 全部步驟可用滑鼠拖曳與手機滑動完成。
- [ ] 教學所示範的操作在正式模式得到相同結果。
- [ ] 錯誤方向、點擊、拖出棋盤不會卡住教學，可重試。
- [ ] 不以棋盤座標作為主要說明，玩家可依高亮與手勢理解。
- [ ] 略過、重新教學與正式開局的導向正常。

## P4：計分、訂單與五關難度

### 實作範圍

1. 以「每格基礎分＋特殊生成獎勵＋連鎖倍率」取代 8 格以上固定基礎分；公式集中配置並可驗證。
2. 初始計分方案：每清除一個食物 10 分；生成炸彈獎勵 20 分、棉花糖 50 分；連鎖倍率從 1 倍開始每波增加 0.5，上限 3 倍。生成獎勵不乘倍率，每格每波只計一次。
3. 加入指定食物訂單。普通配對與特殊效果清除都計入收集；炸彈依原食物種類計入，棉花糖不計入食物收集。
4. 加入單層紙袋障礙：占用格子、不能交換，鄰接消除或爆炸可移除；移除後成為可補入食物的格子。棉花糖單獨清除食物不直接移除紙袋；全盤清除可以。紙袋不計為食物分數。
5. 關卡設定支援多種目標；HUD 顯示個別進度，所有必要目標達成才通關。
6. 五關定位如下，確切目標數量與步數於測試後定稿。

| 關卡 | 目標與引導 | 預期策略 |
| --- | --- | --- |
| 逢甲 | 單純得分 | 三消、連鎖與基本操作 |
| 饒河 | 指定兩種食物訂單 | 有目的地選擇交換 |
| 東大門 | 清除少量紙袋 | 在指定區域消除、利用炸彈 |
| 花園 | 訂單＋紙袋 | 保留與安排特殊方塊 |
| 武聖 | 兩階段訂單；共用步數、保留棋盤 | 規劃多步與特殊組合 |

7. 武聖每次有效交換及連鎖結束後才推進訂單階段；已完成階段的超額收集不回填下一階段，轉階段時清楚提示。
8. 記錄本地測試資料：種子、有效步數、勝負、剩餘步數、單步得分、特殊生成與觸發次数。固定種子覆蓋一般與極端盤面，簡單策略模擬協助比對，再以人工遊玩判斷手感。

### 驗收清單

- [ ] 同 Combo 下清除更多格確實得到更多分，製造特殊方塊有額外獎勵。
- [ ] 收集、障礙與混合目標進度正確，不重複計數。
- [ ] 紙袋不造成重力穿越錯誤或永久不可解區域；洗牌能處理障礙盤面。
- [ ] 第一關測試方向為約 6～10 次有效交換；第二至五關逐步需要更多規劃，不再普遍兩三步結束。
- [ ] 以每關至少 30 個固定種子的模擬結果及至少 5 局人工遊玩作為初次調整資料；模擬勝率不當作真人勝率。
- [ ] 第五關較前四關有挑戰，但不是只能靠罕見超長連鎖通關。
- [ ] 最後一步達標判勝利；多階段轉換、重試與結算沒有錯誤。

## P5：無限夜市巡迴

### 實作範圍

1. 通過第五關解鎖「夜市巡迴」，在首頁、第五關結算與關卡選單提供入口。
2. 獨立記錄巡迴站數，不把無限關卡硬塞入現有五關陣列。
3. 每站使用種子與模板生成，輪替得分、收集、清障礙及混合目標；每五站為挑戰站，沿用現有夜市場景。
4. 以已驗證模板調整目標與障礙，難度分段提高後進入循環變化，設定步數下限、障礙上限及目標上限，避免無限提高數值。
5. 生成時檢查合法交換、目標食物可生成、障礙可接近。檢查失敗時使用已驗證備用模板；不宣稱僅靠這些檢查即可數學保證必勝。
6. 每站開始及每次有效交換結算後保存穩定盤面、亂數狀態、步數、目標進度與分數。刷新後可續玩；不保存動畫中的過渡狀態。
7. 失敗可重試當站，使用相同初始種子；保存最高完成站數，主線解鎖不受影響。

### 驗收清單

- [ ] 第五關後可進入巡迴，既有已通關存檔也能解鎖。
- [ ] 第 1、5、6、25、100、1000 站設定有效，沒有目標溢出或負步數。
- [ ] 連續完成至少 20 站的流程測試，不會遇到沒有下一站的斷點。
- [ ] 自動生成樣本涵蓋各模板及難度邊界；不出現無法生成的收集目標或不可接近障礙。
- [ ] 刷新、離開再回來可恢復穩定狀態；失敗重試不跳站。
- [ ] 存檔損毀或瀏覽器禁止儲存時，遊戲仍可開始並清楚提示無法保存。

## P6：重玩誘因與整體驗收

### 實作範圍

1. 主線加入一至三星：通關至少一星，額外星等依剩餘步數比例配置，各關門檻可調且顯示清楚。
2. 保存各關最高分、最高星等、最佳剩餘步數與最高連鎖；巡迴保存最高完成站數。
3. 結算呈現目標成果、特殊方塊使用與最高連鎖；星等更新不倒退。
4. 閒置後提示一組合法交換，可關閉；提示必須遵守特殊交換與障礙規則，不宣稱是最佳解。
5. 檢查首頁 → 教學 → 五關 → 巡迴 → 續玩全流程，以及窄螢幕、觸控、音量與減少動態效果。
6. 更新 README 的玩法、關卡目標、特殊規則、存檔與啟動方式。

### 驗收清單

- [ ] 重玩失敗或取得較低成績不覆蓋個人最佳紀錄。
- [ ] 星等門檻與結算一致，玩家能知道下一顆星的取得方式。
- [ ] 提示可實際執行，動畫與操作期間不干擾玩家。
- [ ] 完整流程沒有阻斷性錯誤，手機操作及 HUD 不溢出。
- [ ] 舊存檔、新存檔、無存檔與儲存不可用四種情境均驗證。
- [ ] 文件與實際玩法一致，未完成項目明確列出。

## 本輪不納入的擴充

下列項目保留為後續企劃，不作為 P1～P6 驗收必要條件：線上排行榜、帳號與雲端存檔、付費道具、每日任務、永久能力成長、更多食物種類、T／L 專屬特殊方塊、整批食物轉炸彈、額外夜市場景美術。

## 階段交付紀錄格式

每階段結束更新本文件並附上：

- 狀態：未開始／實作中／待驗收／驗收完成。
- 已完成範圍與主要變更檔案。
- 自動驗證、人工驗證及尚無法驗證的項目。
- 使用者可重現的驗收操作步驟。
- 已知問題、調整過的規則或數值與原因。
- 使用者驗收結果及下一階段。

目前下一步：驗收 P2「棋盤辨識與特殊特效」，確認後再開始 P3。本次依使用者指示進入 P2，P1 歷史驗收紀錄保留。

## P1 交付紀錄（2026-09-23）

狀態：待驗收。上方 P1 驗收核取方塊保留給實際驗收確認，未直接以自動測試取代人工驗收。

### 已完成

- `js/game-engine.js`：不依賴 DOM 的棋盤引擎。統一方塊為 `{ color, special }`，抽出配對、合法交換、特殊效果佇列、生成保護、掉落補牌、計分與單步勝負判定。
- `index.html`：普通交換與特殊交換共用逐波結算；每局識別阻止舊交換、清除、掉落及入場動畫修改新局。取消觸控、失去捕捉、非主要指標與重複交換均有處理。
- 無解開局／結算後無解自動洗牌。最多嘗試 120 次，仍失敗則保留特殊物件、重建普通食物並提供可交換組合；不計分、不扣步。若自訂異常盤面仍無法恢復，保留原盤並提供重試，不無限迴圈。
- `js/progress-store.js`：沿用舊解鎖資料，寫入版本 1 存檔；資料損毀可回退舊資料，儲存不可用時保留本頁記憶體進度。
- 支援 `createEngine({ seed })`、`setBoard(...)`、`setRefill(...)` 與亂數狀態讀寫，供後續教學與重現測試。
- 使用傳統外部 script，沒有新增執行期套件或 ES module／fetch 載入需求。

### 驗證結果與限制

- 執行 `node --test tests/engine.test.cjs tests/page.test.cjs`：35 項測試通過（含交換動畫問題的 3 項回歸測試）。
- 引擎：固定盤面涵蓋三／四／五消、T 形去重、各種特殊組合、同波生成保護、最多食物與同數量選擇、重力補牌、洗牌與有限次備援。
- 500 個種子的開局無預先配對且至少有一步合法交換；另以相同種子完成 30 次交換，比對每波與補牌可重現。
- 實際頁面腳本在模擬 DOM／可控制計時器下驗證：無效交換不扣步、多波只扣一步、洗牌不額外得分扣步、交換／清除／掉落期間換局、快速換關、觸控取消、最後一步通關、五關設定與資源存在。
- 檔案 URL 與 HTTP URL 已在模擬環境驗證腳本啟動；**尚未完成真實瀏覽器的直接開檔、靜態伺服器、觸控、音樂播放與視覺驗收**。瀏覽器工具回報 `Codex auth token is unavailable`，無法連線；模擬 DOM 不代表瀏覽器實測。

### 使用者驗收步驟

1. 直接開啟 `index.html`，開始第一關。確認棋盤、圖片正常；滑動可消除，有效操作連鎖結束後只扣一步。
2. 嘗試無法三消的普通交換，確認退回、不扣步；快速連續滑動不會多扣步或卡住。
3. 在入場、交換、消除或掉落期間按「返回首頁」，立即開始新局，重複數次。新局應保持初始分數與步數，沒有舊結算跳出。
4. 製造四消與五消，確認分別生成炸彈、棉花糖。炸彈交換後清除周圍 3×3；棉花糖與食物交換清除同種類。新生成特殊方塊當波應保留。
5. 在原本使用的網址／瀏覽器開啟關卡選單，確認舊解鎖進度仍在。完成一關、重新載入，確認解鎖保存。檔案 URL 與不同伺服器來源的 localStorage 本來就分開，不要求跨來源自動搬移。
6. 透過 IDE 的靜態伺服器重做開始遊戲、交換、重試與換關；再以手機或觸控裝置確認滑動及取消觸控正常，背景音樂能切換。
7. 罕見特殊組合、無解盤面與存檔異常以測試檔內的固定案例重現，不需靠隨機遊玩等待。

### 本階段設計界線

- 保留原五關分數、步數與原計分公式；P4 才調整難度與計分。
- 特殊規則依本企劃 P1 固定規則實作；炸彈的食物底色、強化特效與教學更新分別留在 P2、P3。
- 洗牌備援可能改變普通食物數量分布；特殊方塊種類與食物屬性保留。這是有限次嘗試後恢復遊戲的策略。
- 使用者驗收結果：初步遊玩回報正常；另回報既有的單邊交換動畫問題，已修正，待複驗。後續階段：P2，尚未開始。

### 追加修正：交換時只有一邊移動

- 原因：掉落後 `tile-drop`／`tile-drop-new` class 沒有移除。CSS 的 `animation-fill-mode: both` 持續保留最終 transform，且掉落樣式中的 `transition: none` 蓋過交換 transition；當兩格只有一格保留掉落狀態，就出現單邊移動。
- 修正：每波掉落等待結束、確認仍為同一局後，移除掉落 class、延遲與位移參數；交換前也清理兩個參與方塊的殘留掉落狀態，再測量位置及開始交換。
- 驗證：新增固定補牌的完整掉落流程測試，以及一般掉落／新補牌兩種單邊殘留情境。3 項測試修正前失敗、修正後通過；全部 35 項測試通過。模擬 DOM 驗證狀態清理，未宣稱已在真實瀏覽器量測動畫。
- 複驗方式：消除一次後，將剛掉落或新補入的食物與旁邊未移動的食物交換，橫向及縱向都試，並確認無效交換時兩邊都能移動並退回。

## P2 交付紀錄（2026-09-23）

狀態：待驗收。程式與自動驗證完成，上方核取方塊仍保留給使用者驗收。

### 已完成

- `js/board-visuals.js`、`board-visuals.css`：方塊統一為底色、食物、標記、效果四層。珍奶／地瓜球／臭豆腐／雞排／蚵仔煎依序使用紫／金黃／青綠／珊瑚紅／藍色，底色透明度 26%。
- 炸彈保留原食物圖案、底色及名稱，右下角使用章魚燒小徽章；待機有脈衝光圈與兩點火星。棉花糖使用獨立圖案、彩虹底與旋轉環。
- 生成時顯示白框與星記；發動時顯示清除格、引爆來源及同類目標連線，與待機效果區分。
- `js/game-engine.js`：每波回傳特殊效果來源與實際目標；previewSwap 使用複製盤面，預覽不改動正式棋盤、亂數或補牌佇列。特殊連鎖沿用原引擎規則。
- `index.html`：拖曳超過原有 20px 門檻時顯示交換後第一波完整範圍（包含波及特殊方塊）；特殊格拖向普通格，或普通格拖向特殊格都支援。不預測掉落後的後續配對。
- 清除特效在掉落前移除；取消拖曳、拖回門檻內、無效方向、返回首頁或換局都清除預覽。特效層不攔截指標事件。
- 動態設定提供「依系統偏好／減少動態／完整效果」，預設跟隨系統且即時回應偏好變更；選擇儲存在獨立本機設定，儲存不可用仍可操作。
- 減少動態時停止旋轉、脈衝、震動、交換／掉落等位移與粒子；保留特殊徽章、生成白框、發動位置、目標格及靜態連線。
- 裝飾粒子受全層 72 個節點預算限制（粒子不超過 72），每波浮字最多 6 個；目標連線去重後最多 64 條，完整目標格不因節流而省略。

### 驗證結果與限制

- `node --test tests/engine.test.cjs tests/page.test.cjs`：42 項全部通過，包含原 35 項回歸與新增 7 項 P2 測試。
- 新增驗證涵蓋五種特殊交換及波及特殊方塊的預覽／實際結果一致、盘面與 RNG／補牌不變、炸彈食物屬性、拖曳普通格向特殊格、邊界與取消清理、全盤效果上限、離開時清理、減少動態保留標記、系統偏好變更與儲存失敗。
- 瀏覽器工具的 URL 安全政策拒絕開啟本機 file URL，因此未完成真實瀏覽器畫面驗收，也未改用其他管道繞過限制。桌面與 360px 辨識度、原生觸控與實機效能仍待人工確認；模擬 DOM 測試不代表視覺或效能實測。
- 已加入 400px 以下的棋盤內距與格距調整，但尚未以真實 360px 畫面確認。

### 使用者驗收步驟

1. 直接開啟 `index.html`，開始第一關；確認五種食物有不同底色且圖案清晰。將視窗改為 360px 寬，檢查棋盤與設定沒有溢出。
2. 製造四消；生成格應有短暫白框星記，之後呈現原食物與章魚燒徽章、脈衝光圈。五消應生成具有旋轉彩虹環的棉花糖。
3. 按住炸彈向鄰格拖曳，先不放開，確認虛線目標出現在交換後位置的範圍。放開後實際清除應與預覽一致；邊角的 3×3 會被棋盤邊界裁切。
4. 棉花糖向食物拖曳時，確認目標連線；也試普通食物拖向棉花糖。拖回起點或取消手勢後，預覽應消失且不扣步。
5. 使用雙炸彈、棉花糖＋炸彈與雙棉花糖，確認聯集範圍／同類目標／全盤顯示，結算後沒有殘留遮罩。固定案例可參考測試檔的 P2 預覽與全盤清除測試。
6. 將棋盤下方「動態效果」改為「減少動態」，重做生成與發動，確認無旋轉、震動及粒子，但位置與目標仍清楚；重新載入確認選擇保留。切回「依系統偏好」後可用作業系統設定驗證。
7. 在特殊發動期間返回首頁並重開，確認特效與分數沒有沿用到新局；以實際手機再檢查滑動、取消與連續操作。

### 設計界線與下一階段

- 保留原五關、計分、步數、特殊規則及解鎖存檔，沒有調整平衡數值。
- P2 套用正式遊戲；教學共用呈現與手勢的整合依企劃留在 P3。
- 使用者驗收結果：待驗收。下一階段：P3，尚未開始。

## P2 追加修訂前：素材整理（2026-09-23）

- 基準版本已 commit：`0ac8371`（原 P2 棋盤辨識與特殊效果），可用於比較和回退；沒有推送遠端。
- 第五種食物改為烤玉米，正式遊戲、教學名稱與圖片均同步；色號仍為 4、底色仍為藍色，避免與金黃地瓜球混淆。原解鎖存檔不變。
- 原始 PNG 全數保留；雞排、地瓜球四消圖經內建 image_gen 去背，透明衍生主檔另存 `assets/images/cutouts/`。完整提示詞與製作紀錄見 `tools/asset-review/README.md`。
- 13 張食物素材（含五種普通、五種四消、章魚燒、棉花糖及舊蚵仔煎）統一為 256×256 WebP、236px 內容上限與置中透明留白。舊蚵仔煎保留為備用素材，不再由遊戲載入。
- 六張背景原本已是最長邊 1600px、quality 72；由原 PNG 重建後結果一致，不額外降低品質。五格精靈圖更新為 1280×256，末格為烤玉米，修正原設定誤寫七格的問題。
- 全部 19 張 WebP 加精靈圖約 1.78 MB；對應原 PNG 約 31.14 MB。此比較是素材來源與遊戲輸出，不代表本次網頁下載量降低同樣比例，因先前已有 WebP。
- 已檢查深淺背景合成與 40px 縮圖，沒有兩張原圖的棋盤格背景；輸出透明通道與尺寸經程式驗證。來源 SHA-256、大小與尺寸見 `tools/asset-review/manifest.json`。
- 規則與頁面測試共 43 項通過。真實瀏覽器限制延續前述紀錄；本次素材預覽不等於實機遊玩驗收。
- 四消直線消除、T／L／十字章魚燒、組合規則是後續 P2 追加修訂範圍，本次沒有改動引擎規則或讓四消圖提早顯示成尚未實作的效果。
